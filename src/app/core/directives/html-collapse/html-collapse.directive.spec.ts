import { HtmlCollapseDirective } from './html-collapse.directive';

describe('HtmlCollapseDirective', () => {
  it('should create an instance', () => {
    const directive = new HtmlCollapseDirective();
    expect(directive).toBeTruthy();
  });
});
