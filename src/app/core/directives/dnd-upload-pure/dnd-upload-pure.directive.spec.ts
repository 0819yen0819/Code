import { DndUploadPureDirective } from './dnd-upload-pure.directive';

describe('DndUploadPureDirective', () => {
  it('should create an instance', () => {
    const directive = new DndUploadPureDirective();
    expect(directive).toBeTruthy();
  });
});
